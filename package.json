{
  "name": "ns-media-hero-showcase",
  "version": "0.01",
  "description": "WordPress plugin for hero media showcase with zoom animations",
  "main": "assets/js/frontend.js",
  "scripts": {
    "build": "esbuild admin/assets/block-editor.js --bundle --outfile=build/block-editor.js --target=es2015 --minify",
    "build:dev": "esbuild admin/assets/block-editor.js --bundle --outfile=build/block-editor.js --target=es2015 --sourcemap",
    "watch": "esbuild admin/assets/block-editor.js --bundle --outfile=build/block-editor.js --target=es2015 --sourcemap --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "wordpress",
    "plugin",
    "hero",
    "showcase",
    "media",
    "gallery",
    "animation"
  ],
  "author": "Netservice",
  "license": "GPL-2.0-or-later",
  "devDependencies": {
    "esbuild": "^0.19.0"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/netservice/ns-media-hero-showcase.git"
  },
  "bugs": {
    "url": "https://github.com/netservice/ns-media-hero-showcase/issues"
  },
  "homepage": "https://netservice.jp/"
}